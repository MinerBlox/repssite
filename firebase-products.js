import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.5/firebase-app.js";
import { enableAppCheck } from "./firebase-app-check.js?v=2026-06-30-app-check-1";
import { getFirestore, collection, getDocs, getCountFromServer, query, where, orderBy, limit, startAfter } from "https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js";

const firebaseConfig={apiKey:"AIzaSyDTTzoJlvr0mYxwx82cQ9JJn8rXrMEy7JA",authDomain:"reps-central.firebaseapp.com",projectId:"reps-central",storageBucket:"reps-central.firebasestorage.app",messagingSenderId:"812299387060",appId:"1:812299387060:web:1c93d1e7bf30b05653d7e1",measurementId:"G-8T7F9F1FZ9"};
const app=initializeApp(firebaseConfig);enableAppCheck(app);const db=getFirestore(app);
const HIDDEN_CATEGORY="uncategorised",PAGE_SIZE=100,CATEGORY_SAMPLE_SIZE=10,CURRENCY_KEY="rc-currency",RATE_CACHE_KEY="rc-cny-rates";
const FALLBACK_FILTER_CATEGORIES=["Shoes","Hoodies","Tees","Shorts","Sweats","Jeans","Jackets","Puffer","Sweaters","Sets","Jerseys","Accessories","Room Decor","Electronics","Womens"];
const FALLBACK_CURRENCIES={CNY:"Chinese Yuan",GBP:"British Pound",USD:"United States Dollar",EUR:"Euro",AUD:"Australian Dollar",CAD:"Canadian Dollar",JPY:"Japanese Yen",KRW:"South Korean Won",HKD:"Hong Kong Dollar",SGD:"Singapore Dollar",NZD:"New Zealand Dollar",CHF:"Swiss Franc"};
const FALLBACK_RATES={CNY:1,GBP:.103,USD:.139,EUR:.119,AUD:.212,CAD:.190,JPY:21.8,HKD:1.09,SGD:.178,CHF:.111,NZD:.232,KRW:191.5};
let firebaseItems=[],visibleItems=[],renderedCount=0,selectedCategory="All",searchTerm=new URLSearchParams(location.search).get("search")||"";
const requestedBrand=new URLSearchParams(location.search).get("brand");let selectedBrand=requestedBrand||sessionStorage.getItem("rc-pending-brand")||"";sessionStorage.removeItem("rc-pending-brand");
let selectedCurrency=localStorage.getItem(CURRENCY_KEY)||"",currencyNames={...FALLBACK_CURRENCIES},cnyRates={...FALLBACK_RATES},filterCategories=[...FALLBACK_FILTER_CATEGORIES],categoryCounts=new Map(),countsReady=false,lastAllDoc=null,allFinished=false,loadingPage=false,fullSearchLoaded=false,searchTimer=0,renderFrame=0;
const categoryCache=new Map(),categoryPreviewCache=new Map();
const style=document.createElement("style");style.textContent=`@media(min-width:721px){.category-row{overflow:visible!important}.category-chips{flex-wrap:wrap!important;overflow-x:visible!important}}`;document.head.appendChild(style);
const esc=v=>String(v||"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#39;");
const hidden=v=>String(v||"").trim().toLowerCase()===HIDDEN_CATEGORY;
function safeUrl(v){try{const u=new URL(String(v||""),location.href);return /^https?:$/.test(u.protocol)?u.href:""}catch{return""}}
function formatMoney(v,c){try{return new Intl.NumberFormat(undefined,{style:"currency",currency:c,currencyDisplay:"narrowSymbol"}).format(v)}catch{return`${c} ${Number(v).toFixed(2)}`}}
function priceMarkup(i){const y=Number(i.price||0),c=selectedCurrency||"CNY",r=c==="CNY"?1:Number(cnyRates[c]||1);return`<span class="product-price-stack"><span class="product-price">${formatMoney(y*r,c)}</span><span class="yuan-price">~ ¥${y.toFixed(2)}</span></span>`}
function imageMarkup(i){const u=safeUrl(i.imageUrl);return u?`<img class="product-image" src="${esc(u)}" alt="${esc(i.name||"Product image")}" loading="eager" decoding="async">`:`<svg width="42" height="42" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/></svg>`}
function badgeLabel(t){return t==="best"?"Best Batch":t==="budget"?"Budget":t==="new"?"New":t==="popular"?"Popular":""}
function card(i){const href=i.id?`items/${encodeURIComponent(i.id)}/`:"#",agent=safeUrl(i.agentUrl);return`<article class="product-card" data-product-id="${esc(i.id)}"><div class="product-top"><span class="item-badge ${esc(i.badge||"")}">${badgeLabel(i.badge)}</span>${imageMarkup(i)}</div><div class="product-body"><div class="product-name">${esc(i.name||"Unnamed item")}</div><div class="product-meta">${priceMarkup(i)}<span class="product-category">${esc(i.category||"Unsorted")}</span></div><div class="product-actions"><a href="${esc(agent||href)}" class="product-btn primary" data-agent-product="${esc(i.id)}"${agent?' target="_blank" rel="noopener noreferrer"':''}>Link</a><a href="${esc(href)}" class="product-btn" data-view-product="${esc(i.id)}">View Details →</a></div></div></article>`}
function normalize(docs){return docs.map(d=>({id:d.id,...d.data()})).filter(i=>i.isActive!==false&&!hidden(i.category))}
function merge(items){const m=new Map(firebaseItems.map(i=>[i.id,i]));items.forEach(i=>m.set(i.id,i));firebaseItems=[...m.values()]}
function filtered(){const q=searchTerm.trim().toLowerCase();return firebaseItems.filter(i=>!hidden(i.category)&&(selectedCategory==="All"||i.category===selectedCategory)&&(!selectedBrand||String(i.brand||"").toLowerCase()===selectedBrand.toLowerCase())&&(`${i.name||""} ${i.brand||""} ${i.category||""} ${(i.tags||[]).join(" ")}`.toLowerCase().includes(q)))}
function totalForCurrent(){if(searchTerm.trim()||selectedBrand)return filtered().length;if(countsReady){if(selectedCategory!=="All")return Number(categoryCounts.get(selectedCategory)||0);return[...categoryCounts.values()].reduce((a,b)=>a+Number(b||0),0)}return selectedCategory==="All"?Math.max(firebaseItems.length,visibleItems.length):Math.max((categoryPreviewCache.get(selectedCategory)||[]).length,visibleItems.length)}
function updateMeta(){const c=document.getElementById("results-count"),copy=document.getElementById("results-copy"),pill=document.getElementById("active-filter-pill"),total=totalForCurrent();if(c)c.textContent=`${total} item${total===1?"":"s"}`;if(pill)pill.textContent=selectedBrand?`Brand: ${selectedBrand}`:`Category: ${selectedCategory}`;if(copy){if(searchTerm.trim())copy.textContent=`Showing results for “${searchTerm.trim()}”.`;else if(selectedCategory!=="All")copy.textContent=`Browse ${selectedCategory}.`;else copy.textContent="Browse all items."}}
function renderChips(){const w=document.getElementById("category-chips");if(!w)return;const cats=filterCategories.filter(c=>c&&!hidden(c));w.innerHTML=["All",...new Set(cats)].map(c=>`<button class="category-chip ${selectedCategory===c?"active":""}" type="button" data-category="${esc(c)}">${esc(c)}</button>`).join("")}
function columns(){const g=document.getElementById("product-grid");if(!g)return 1;const w=g.clientWidth;return w<520?1:w<720?2:Math.max(1,Math.floor((w+16)/236))}
function rowHeight(){const g=document.getElementById("product-grid"),f=g?.querySelector(".product-card");if(!f)return 360;const gap=parseFloat(getComputedStyle(g).rowGap||getComputedStyle(g).gap)||16;return Math.max(160,f.getBoundingClientRect().height+gap)}
function targetCount(){const g=document.getElementById("product-grid");if(!g)return 0;const top=g.getBoundingClientRect().top+scrollY,depth=Math.max(innerHeight,scrollY+innerHeight-top);return(Math.ceil(depth/rowHeight())+2)*columns()}
function appendUntilTarget(){const g=document.getElementById("product-grid");if(!g)return;const t=Math.min(visibleItems.length,targetCount());if(t>renderedCount){g.insertAdjacentHTML("beforeend",visibleItems.slice(renderedCount,t).map(card).join(""));renderedCount=t}updateMeta()}
function resetRendered(){const g=document.getElementById("product-grid");if(!g)return;visibleItems=filtered();renderedCount=0;g.innerHTML="";const e=document.getElementById("empty-state");if(e)e.style.display=visibleItems.length?"none":"block";appendUntilTarget()}
async function loadCategories(){try{const s=await getDocs(collection(db,"categories")),stored=s.docs.map(d=>d.data().name||d.id).filter(c=>c&&!hidden(c));filterCategories=[...new Set([...FALLBACK_FILTER_CATEGORIES,...stored])]}catch{}renderChips();void preloadCategoryData()}
async function preloadCategoryData(){const counts=new Map();await Promise.all(filterCategories.filter(c=>!hidden(c)).map(async category=>{try{const [sample,count]=await Promise.all([getDocs(query(collection(db,"liveproducts"),where("category","==",category),limit(CATEGORY_SAMPLE_SIZE))),getCountFromServer(query(collection(db,"liveproducts"),where("category","==",category)))]);categoryPreviewCache.set(category,normalize(sample.docs));counts.set(category,count.data().count||0)}catch{categoryPreviewCache.set(category,[]);counts.set(category,0)}}));categoryCounts=counts;countsReady=true;renderChips();updateMeta()}
async function loadInitial(){loadingPage=true;try{const s=await getDocs(query(collection(db,"liveproducts"),orderBy("sortOrder","asc"),limit(PAGE_SIZE)));lastAllDoc=s.docs.at(-1)||null;allFinished=s.docs.length<PAGE_SIZE;firebaseItems=normalize(s.docs)}catch{const s=await getDocs(query(collection(db,"liveproducts"),limit(PAGE_SIZE)));lastAllDoc=s.docs.at(-1)||null;allFinished=s.docs.length<PAGE_SIZE;firebaseItems=normalize(s.docs)}finally{loadingPage=false}resetRendered();document.getElementById("spreadsheet-loading")?.style.setProperty("display","none");document.getElementById("product-grid")?.style.setProperty("display","grid")}
async function loadNextPage(){if(loadingPage||allFinished||selectedCategory!=="All"||searchTerm.trim()||selectedBrand)return;loadingPage=true;try{const q=lastAllDoc?query(collection(db,"liveproducts"),orderBy("sortOrder","asc"),startAfter(lastAllDoc),limit(PAGE_SIZE)):query(collection(db,"liveproducts"),orderBy("sortOrder","asc"),limit(PAGE_SIZE)),s=await getDocs(q);if(s.docs.length)lastAllDoc=s.docs.at(-1);if(s.docs.length<PAGE_SIZE)allFinished=true;merge(normalize(s.docs));visibleItems=filtered();appendUntilTarget()}catch{}finally{loadingPage=false}}
async function loadCategory(category){selectedCategory=category;selectedBrand="";searchTerm="";const input=document.getElementById("search-input");if(input)input.value="";renderChips();if(category==="All"){await loadInitial();return}const preview=categoryPreviewCache.get(category)||[];firebaseItems=preview;resetRendered();if(categoryCache.has(category)){firebaseItems=categoryCache.get(category);resetRendered();return}try{const s=await getDocs(query(collection(db,"liveproducts"),where("category","==",category))),items=normalize(s.docs).sort((a,b)=>(Number(a.sortOrder)||0)-(Number(b.sortOrder)||0));categoryCache.set(category,items);firebaseItems=items;resetRendered()}catch{}}
async function ensureFullForSearch(){if(fullSearchLoaded)return;const s=await getDocs(collection(db,"liveproducts"));firebaseItems=normalize(s.docs);fullSearchLoaded=true;resetRendered()}
async function maybeLoadMore(){if(renderFrame)return;renderFrame=requestAnimationFrame(async()=>{renderFrame=0;appendUntilTarget();if(selectedCategory==="All"&&!searchTerm.trim()&&!selectedBrand&&renderedCount>=visibleItems.length-2&&!allFinished){await loadNextPage();appendUntilTarget()}})}
function renderCurrencyList(search=""){const l=document.getElementById("currency-list");if(!l)return;const n=search.trim().toLowerCase();l.innerHTML=Object.entries(currencyNames).filter(([c,name])=>!n||c.toLowerCase().includes(n)||name.toLowerCase().includes(n)).map(([c,name])=>`<button class="currency-option ${c===selectedCurrency?"selected":""}" type="button" data-currency="${c}"><span class="currency-code">${c}</span><span class="currency-name">${name}</span></button>`).join("")}
function openCurrencyPicker(required=false){const o=document.getElementById("currency-overlay"),c=document.getElementById("currency-close");if(!o)return;o.classList.add("open");if(c)c.hidden=required||!selectedCurrency;renderCurrencyList()}
function closeCurrencyPicker(){if(selectedCurrency)document.getElementById("currency-overlay")?.classList.remove("open")}
function chooseCurrency(code){selectedCurrency=code;localStorage.setItem(CURRENCY_KEY,code);document.getElementById("currency-overlay")?.classList.remove("open");const p=document.getElementById("currency-pill");if(p)p.textContent=`Currency: ${code}`;resetRendered()}
async function loadCurrencyData(){try{const cached=JSON.parse(localStorage.getItem(RATE_CACHE_KEY)||"null");if(cached?.rates)cnyRates={...cnyRates,...cached.rates}}catch{}try{const[c,r]=await Promise.all([fetch("https://api.frankfurter.dev/v1/currencies"),fetch("https://api.frankfurter.dev/v1/latest?base=CNY")]);if(c.ok)currencyNames={...currencyNames,...await c.json()};if(r.ok){const d=await r.json();cnyRates={...cnyRates,CNY:1,...d.rates};localStorage.setItem(RATE_CACHE_KEY,JSON.stringify({rates:cnyRates,savedAt:Date.now()}));resetRendered()}}catch{}}
function initializeCurrency(){const p=document.getElementById("currency-pill");if(p)p.textContent=`Currency: ${selectedCurrency||"Select"}`;document.getElementById("currency-search")?.addEventListener("input",e=>renderCurrencyList(e.target.value));document.getElementById("currency-list")?.addEventListener("click",e=>{const o=e.target.closest("[data-currency]");if(o)chooseCurrency(o.dataset.currency)});document.getElementById("currency-close")?.addEventListener("click",closeCurrencyPicker);if(!selectedCurrency)openCurrencyPicker(true);void loadCurrencyData()}
const searchInput=document.getElementById("search-input");if(searchInput){searchInput.value=searchTerm;searchInput.addEventListener("input",e=>{searchTerm=e.target.value;clearTimeout(searchTimer);searchTimer=setTimeout(async()=>{if(searchTerm.trim())await ensureFullForSearch();resetRendered()},220)})}
document.getElementById("category-chips")?.addEventListener("click",e=>{const chip=e.target.closest("[data-category]");if(chip)void loadCategory(chip.dataset.category)});
document.getElementById("product-grid")?.addEventListener("click",e=>{const a=e.target.closest("[data-agent-product]");if(a){window.rcTrackProductInteraction?.(a.dataset.agentProduct,"outboundClicks");return}const v=e.target.closest("[data-view-product]");if(v)window.rcTrackProductInteraction?.(v.dataset.viewProduct,"viewClicks")});
window.addEventListener("scroll",maybeLoadMore,{passive:true});window.addEventListener("resize",maybeLoadMore);window.setCategory=loadCategory;window.clearCategory=()=>loadCategory("All");window.openCurrencyPicker=openCurrencyPicker;window.copyProductLink=async(url,id)=>{if(url&&url!=="#"){await navigator.clipboard.writeText(new URL(url,location.href).href);window.rcTrackProductInteraction?.(id,"copyClicks")}};
initializeCurrency();renderChips();void loadCategories();await loadInitial();if(searchTerm.trim()||selectedBrand){await ensureFullForSearch();resetRendered()}
